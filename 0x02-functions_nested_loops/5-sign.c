#include <stdio.h>
/**
 * _islower - nested loops
 *
 * Description: putchar
 * Return: 0 when it runs as expected
 * Return: 1 when lowercase
 */
int print_sign(int n)
{
	int xy;
	int ch;
	int lm;

	if (n > 0)
	{
		xy = int(+1);
		return (xy);
	}
	else if (n == 0)
	{
		ch = int(000);
		return (ch);
	}
	else
	{
		lm = int(-1);
		return (lm);
	}
}

