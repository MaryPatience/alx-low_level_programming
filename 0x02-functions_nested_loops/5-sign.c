#include <stdio.h>
/**
 * _islower - nested loops
 *
 * Description: putchar
 * Return: 0 when it runs as expected
 * Return: 1 when lowercase
 */
int print_sign(int n)
{
	int x;
	int m;

	if (n > 0)
	{
		x = +1;
		return x;
	}
	else if (n == 0)
	{
		return (00);
	}
	else
	{
		m = -1;
		return m;
	}
}

