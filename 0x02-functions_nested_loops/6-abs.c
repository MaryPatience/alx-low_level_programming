#include <stdio.h>
#include "main.h"
#include <stdlib.h>
#include <math.h>
/**
 * _abs - nested loops
 * @c: the int to print
 *
 * Description: putchar
 * Return: 0 when it runs as expected
 */
int _abs(int)
{
	int c;

	putchar(abs(c) + '0');
	return (0);
}
